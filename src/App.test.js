// import { render } from '@testing-library/react';
import '@testing-library/jest-dom';
// import App from './App';

describe('App component', () => {
  test('renders App component', () => {
    ///
    // const { getByText } = render(<App />);
    // expect(getByText('EXPLORE DEMO NOW')).toBeInTheDocument();
  });
});
